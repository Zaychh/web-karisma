import SidebarOnly from "../../SidebarOnly";
import Boot from "./bootpage";

export default function Bootcamp () {
    return (
        <SidebarOnly>
            <Boot />
        </SidebarOnly>
    );
}