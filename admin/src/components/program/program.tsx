import SidebarOnly from "../../SidebarOnly";
import Program from "./propage";

export default function ProgramPage() {
    return (
        <SidebarOnly>
            <Program />
        </SidebarOnly>
    );
}